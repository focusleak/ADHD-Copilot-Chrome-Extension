{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "React",
            "type": "chrome",
            "request": "attach",
            "url": "http://localhost:8004",
            "webRoot": "${workspaceFolder}/src"
        }
    ]
}